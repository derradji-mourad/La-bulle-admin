import { motion } from "framer-motion";
import { Check, ArrowRight, Sparkles } from "lucide-react";
import { Link } from "react-router-dom";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import SectionHeading from "@/components/SectionHeading";

interface PricingCardProps {
  title: string;
  price: string;
  unit: string;
  features: string[];
  popular?: boolean;
  persona?: string;
  delay?: number;
}

const PricingCard = ({ title, price, unit, features, popular, persona, delay = 0 }: PricingCardProps) => (
  <motion.div
    initial={{ opacity: 0, y: 30 }}
    whileInView={{ opacity: 1, y: 0 }}
    viewport={{ once: true }}
    transition={{ delay, duration: 0.5 }}
    whileHover={{ y: -8, transition: { duration: 0.25 } }}
    className={`rounded-2xl p-8 border relative transition-all duration-300 ${
      popular
        ? "border-primary bg-card shadow-card-hover scale-[1.02] ring-2 ring-primary/20"
        : "border-border bg-card shadow-card hover:shadow-card-hover hover:border-primary/20"
    }`}
  >
    {popular && (
      <span className="absolute -top-3.5 left-1/2 -translate-x-1/2 bg-primary text-primary-foreground text-xs font-bold px-4 py-1.5 rounded-full flex items-center gap-1.5 shadow-lg shadow-primary/25">
        <Sparkles size={12} /> Le plus populaire
      </span>
    )}
    {persona && (
      <span className="text-xs font-medium text-primary bg-accent px-3 py-1 rounded-full mb-4 inline-block">
        {persona}
      </span>
    )}
    <h3 className="font-heading text-xl font-semibold text-foreground mb-2">{title}</h3>
    <div className="mb-6">
      <span className="font-heading text-5xl font-bold text-foreground">{price}</span>
      <span className="text-muted-foreground text-sm ml-2">{unit}</span>
    </div>
    <ul className="space-y-3 mb-8">
      {features.map((f) => (
        <li key={f} className="flex items-start gap-3 text-sm text-muted-foreground">
          <Check size={18} className="text-primary mt-0.5 shrink-0" />
          <span>{f}</span>
        </li>
      ))}
    </ul>
    <button
      className={`w-full py-3.5 rounded-xl font-semibold text-sm transition-all duration-300 ${
        popular
          ? "bg-primary text-primary-foreground hover:shadow-lg hover:shadow-primary/25"
          : "bg-secondary text-secondary-foreground hover:bg-primary hover:text-primary-foreground"
      }`}
    >
      Réserver maintenant
    </button>
  </motion.div>
);

const coworkingPlans = [
  { title: "Journée", price: "1 500", unit: "DA/jour", persona: "Idéal pour tester", features: ["Accès open space", "Wi-Fi haut débit", "Café & thé inclus", "Casier personnel"] },
  { title: "Mensuel", price: "8 500", unit: "DA/mois", persona: "Best pour freelancers", features: ["Accès illimité", "Wi-Fi haut débit", "Café & thé inclus", "Casier personnel", "Accès salle de réunion (2h/mois)", "Communauté & événements"], popular: true },
  { title: "Bureau Dédié", price: "15 000", unit: "DA/mois", persona: "Best pour startups", features: ["Bureau fixe réservé", "Wi-Fi haut débit", "Café & thé inclus", "Rangement personnel", "Accès salle de réunion (5h/mois)"] },
];

const domiciliationPlans = [
  { title: "Essentiel", price: "10 000", unit: "DA/mois", persona: "Pour démarrer", features: ["Adresse commerciale", "Réception du courrier", "Boîte aux lettres dédiée", "Certificat de domiciliation"] },
  { title: "Premium", price: "18 000", unit: "DA/mois", persona: "Pour scaler", features: ["Adresse commerciale", "Réception du courrier", "Scan & envoi du courrier", "Accès coworking (5j/mois)", "Salle de réunion (3h/mois)"], popular: true },
];

const roomPlans = [
  { title: "Salle de Travail", price: "2 000", unit: "DA/h", persona: "Petites réunions", features: ["Capacité 4 personnes", "Écran de présentation", "Wi-Fi haut débit", "Tableau blanc"] },
  { title: "Salle de Réunion", price: "3 500", unit: "DA/h", persona: "Sessions pro", features: ["Capacité 10 personnes", "Vidéoprojecteur", "Système de visioconférence", "Café & eau offerts"], popular: true },
  { title: "Salle Workshop", price: "5 000", unit: "DA/h", persona: "Événements", features: ["Capacité 20 personnes", "Matériel de présentation", "Configuration modulable", "Pause café incluse"] },
];

const studioPlan = [
  { title: "Studio Podcast", price: "10 000", unit: "DA/session", persona: "Créateurs de contenu", features: ["Studio insonorisé", "Microphones professionnels", "Mixage audio inclus", "Montage basique inclus", "Export HD"], popular: true },
];

const Pricing = () => (
  <div className="min-h-screen bg-background">
    <Navbar />

    <section className="pt-32 pb-20 bg-muted">
      <div className="container mx-auto px-4 text-center">
        <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}>
          <span className="text-primary font-semibold text-sm uppercase tracking-wider">Tarifs</span>
          <h1 className="font-heading text-4xl md:text-5xl font-bold text-foreground mt-2 mb-4">
            Des tarifs <span className="text-gradient">transparents</span>
          </h1>
          <p className="text-muted-foreground text-lg max-w-lg mx-auto mb-6">
            Choisissez la formule adaptée à vos besoins. Sans engagement, sans surprise.
          </p>
          <div className="flex items-center justify-center gap-6 text-sm text-muted-foreground">
            <span className="flex items-center gap-1.5"><Check size={16} className="text-primary" /> Sans engagement</span>
            <span className="flex items-center gap-1.5"><Check size={16} className="text-primary" /> Annulation flexible</span>
            <span className="flex items-center gap-1.5"><Check size={16} className="text-primary" /> Tout inclus</span>
          </div>
        </motion.div>
      </div>
    </section>

    <section className="py-20 bg-background">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Coworking" title="Espaces de coworking" subtitle="Trouvez le plan qui correspond à votre rythme." />
        <div className="grid md:grid-cols-3 gap-8 lg:gap-12 w-full">
          {coworkingPlans.map((p, i) => <PricingCard key={p.title} {...p} delay={i * 0.1} />)}
        </div>
      </div>
    </section>

    <section className="py-20 bg-muted">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Domiciliation" title="Domiciliation commerciale" subtitle="Une adresse prestigieuse pour votre entreprise." />
        <div className="grid md:grid-cols-2 gap-8 lg:gap-12 w-full">
          {domiciliationPlans.map((p, i) => <PricingCard key={p.title} {...p} delay={i * 0.1} />)}
        </div>
      </div>
    </section>

    <section className="py-20 bg-background">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Salles" title="Salles de travail & réunion" subtitle="Des espaces équipés pour chaque occasion." />
        <div className="grid md:grid-cols-3 gap-8 lg:gap-12 w-full">
          {roomPlans.map((p, i) => <PricingCard key={p.title} {...p} delay={i * 0.1} />)}
        </div>
      </div>
    </section>

    <section className="py-20 bg-muted">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Studio" title="Studio de podcast" subtitle="Créez du contenu professionnel." />
        <div className="max-w-sm mx-auto">
          {studioPlan.map((p) => <PricingCard key={p.title} {...p} />)}
        </div>
      </div>
    </section>

    {/* CTA */}
    <section className="py-20 bg-gradient-hero text-primary-foreground">
      <div className="container mx-auto px-4 text-center">
        <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}>
          <h2 className="font-heading text-3xl md:text-4xl font-bold mb-4">Pas encore sûr ?</h2>
          <p className="opacity-80 mb-8 max-w-md mx-auto">Réservez une visite gratuite et découvrez nos espaces en personne.</p>
          <Link
            to="/solutions"
            className="group inline-flex items-center gap-2 bg-primary-foreground text-primary px-8 py-4 rounded-xl font-semibold hover:shadow-lg transition-all duration-300"
          >
            Réserver une visite <ArrowRight size={18} className="group-hover:translate-x-1 transition-transform" />
          </Link>
        </motion.div>
      </div>
    </section>

    <Footer />
  </div>
);

export default Pricing;
