import { motion } from "framer-motion";
import { Link } from "react-router-dom";
import { ArrowRight, Monitor, Building2, Users, Mic, FileText, Wifi, Coffee, Shield, Clock } from "lucide-react";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import SectionHeading from "@/components/SectionHeading";
import heroImg from "@/assets/hero-coworking.jpg";
import privateImg from "@/assets/private-office.jpg";
import studioImg from "@/assets/studio-podcast.jpg";
import meetingImg from "@/assets/meeting-room.jpg";

const whyReasons = [
  { icon: Wifi, title: "Internet Haut Débit", desc: "Fibre optique dédiée pour une connexion rapide et stable." },
  { icon: Coffee, title: "Espace Détente", desc: "Café, thé et espace de détente pour recharger vos batteries." },
  { icon: Shield, title: "Sécurité 24/7", desc: "Accès sécurisé et surveillance pour protéger vos biens." },
  { icon: Clock, title: "Flexibilité Totale", desc: "Accès 7j/7 avec des formules adaptées à vos besoins." },
];

const spaces = [
  { img: heroImg, title: "Coworking", price: "8 500", unit: "DA/mois", desc: "Espace partagé flexible avec bureau et accès aux commodités." },
  { img: privateImg, title: "Bureau Privatif", price: "25 000", unit: "DA/mois", desc: "Bureau fermé tout équipé pour travailler en toute tranquillité." },
  { img: meetingImg, title: "Bureau Équipe", price: "65 000", unit: "DA/mois", desc: "Espace dédié pour votre équipe avec mobilier et services inclus." },
  { img: studioImg, title: "Studio Podcast", price: "10 000", unit: "DA/session", desc: "Studio professionnel avec équipement audio et vidéo haut de gamme." },
  { img: privateImg, title: "Domiciliation", price: "10 000", unit: "DA/mois", desc: "Adresse commerciale prestigieuse avec gestion du courrier." },
];

const Solutions = () => (
  <div className="min-h-screen bg-background">
    <Navbar />

    <section className="pt-32 pb-20 bg-muted">
      <div className="container mx-auto px-4">
        <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="w-full">
          <span className="text-primary font-semibold text-sm uppercase tracking-wider">Nos Solutions</span>
          <h1 className="font-heading text-4xl md:text-5xl font-bold text-foreground mt-2 mb-4">
            Pourquoi c'est <span className="text-gradient">the place to be</span> ?
          </h1>
          <p className="text-muted-foreground text-lg">
            Un Business Hub, c'est toutes les commodités pour un entrepreneur au même endroit.
          </p>
        </motion.div>
      </div>
    </section>

    {/* Why */}
    <section className="py-24 bg-background">
      <div className="container mx-auto px-4">
        <div className="grid md:grid-cols-4 gap-6">
          {whyReasons.map((item, i) => (
            <motion.div
              key={item.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.1 }}
              className="text-center p-6"
            >
              <div className="w-14 h-14 rounded-full bg-accent flex items-center justify-center mx-auto mb-4">
                <item.icon size={24} className="text-primary" />
              </div>
              <h3 className="font-heading font-semibold text-foreground mb-2">{item.title}</h3>
              <p className="text-muted-foreground text-sm">{item.desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>

    {/* Explore Spaces */}
    <section className="py-24 bg-muted">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Explore Spaces" title="Nos espaces" subtitle="Choisissez la formule qui correspond à vos besoins." />
        <div className="grid md:grid-cols-3 gap-6">
          {spaces.map((space, i) => (
            <motion.div
              key={space.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.08 }}
              className="group bg-card border border-border rounded-xl overflow-hidden shadow-card hover:shadow-card-hover transition-all duration-300"
            >
              <div className="aspect-[16/10] overflow-hidden">
                <img src={space.img} alt={space.title} loading="lazy" className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
              </div>
              <div className="p-6">
                <h3 className="font-heading text-lg font-semibold text-foreground mb-1">{space.title}</h3>
                <p className="text-muted-foreground text-sm mb-4">{space.desc}</p>
                <div className="flex items-end justify-between">
                  <div>
                    <span className="text-sm text-muted-foreground">À partir de</span>
                    <p className="font-heading text-2xl font-bold text-primary">{space.price} <span className="text-sm font-normal text-muted-foreground">{space.unit}</span></p>
                  </div>
                  <Link to="/pricing" className="text-primary text-sm font-semibold hover:underline flex items-center gap-1">
                    Plus <ArrowRight size={14} />
                  </Link>
                </div>
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>

    <Footer />
  </div>
);

export default Solutions;
