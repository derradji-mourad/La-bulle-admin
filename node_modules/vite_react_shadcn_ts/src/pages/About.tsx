import { motion } from "framer-motion";
import { Link } from "react-router-dom";
import { ArrowRight, Mic, Calculator, Scale, Cpu, Megaphone, MapPin } from "lucide-react";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import SectionHeading from "@/components/SectionHeading";
import heroImg from "@/assets/hero-coworking.jpg";
import privateImg from "@/assets/private-office.jpg";
import studioImg from "@/assets/studio-podcast.jpg";
import meetingImg from "@/assets/meeting-room.jpg";

const partners = [
  { icon: Mic, title: "Studio", desc: "Studio de podcast et d'enregistrement professionnel." },
  { icon: Calculator, title: "Comptabilité", desc: "Experts-comptables partenaires." },
  { icon: Scale, title: "Juridique", desc: "Avocats et juristes à votre disposition." },
  { icon: Cpu, title: "Tech", desc: "Développeurs et consultants IT." },
  { icon: Megaphone, title: "Communication", desc: "Agences de communication et marketing." },
];

const gallery = [heroImg, privateImg, studioImg, meetingImg];

const About = () => (
  <div className="min-h-screen bg-background">
    <Navbar />

    {/* Hero */}
    <section className="pt-32 pb-20 bg-muted">
      <div className="container mx-auto px-4">
        <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="w-full">
          <span className="text-primary font-semibold text-sm uppercase tracking-wider">À propos</span>
          <h1 className="font-heading text-4xl md:text-5xl font-bold text-foreground mt-2 mb-4">
            Bienvenue dans <span className="text-gradient">La Bulle</span>
          </h1>
          <p className="text-muted-foreground text-lg">
            Un business hub pensé pour les entrepreneurs algériens. Nous combinons espaces de travail premium et services professionnels pour créer l'écosystème idéal.
          </p>
        </motion.div>
      </div>
    </section>

    {/* Our Spaces */}
    <section className="py-24 bg-background">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Nos espaces" title="Our Offices" subtitle="Des espaces conçus pour la productivité et le bien-être." />
        <div className="grid md:grid-cols-2 gap-8">
          {[
            { img: heroImg, title: "Espace Coworking", desc: "Un open space lumineux et moderne où collaborer et innover au quotidien." },
            { img: privateImg, title: "Bureaux Privatifs", desc: "Des bureaux fermés et équipés pour travailler en toute confidentialité." },
            { img: meetingImg, title: "Salles de Réunion", desc: "Espaces professionnels pour vos réunions, présentations et workshops." },
            { img: studioImg, title: "Studio Podcast", desc: "Un studio d'enregistrement professionnel pour vos contenus audio et vidéo." },
          ].map((space, i) => (
            <motion.div
              key={space.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.1 }}
              className="group rounded-xl overflow-hidden border border-border bg-card"
            >
              <div className="aspect-[16/10] overflow-hidden">
                <img src={space.img} alt={space.title} loading="lazy" className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
              </div>
              <div className="p-6">
                <h3 className="font-heading text-xl font-semibold text-foreground mb-2">{space.title}</h3>
                <p className="text-muted-foreground text-sm">{space.desc}</p>
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>

    {/* Gallery */}
    <section className="py-24 bg-muted">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Galerie" title="Our Bubbles" />
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          {gallery.map((img, i) => (
            <motion.div
              key={i}
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.1 }}
              className="rounded-lg overflow-hidden aspect-square"
            >
              <img src={img} alt="La Bulle gallery" loading="lazy" className="w-full h-full object-cover hover:scale-105 transition-transform duration-300" />
            </motion.div>
          ))}
        </div>
      </div>
    </section>

    {/* Partners */}
    <section className="py-24 bg-background">
      <div className="container mx-auto px-4">
        <SectionHeading badge="Partenaires" title="Our Partners" subtitle="Un réseau d'experts pour vous accompagner." />
        <div className="grid md:grid-cols-5 gap-4">
          {partners.map((p, i) => (
            <motion.div
              key={p.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.05 }}
              className="text-center p-6 rounded-xl border border-border bg-card hover:border-primary/30 transition-colors"
            >
              <p.icon size={28} className="text-primary mx-auto mb-3" />
              <h4 className="font-heading font-semibold text-foreground text-sm">{p.title}</h4>
              <p className="text-muted-foreground text-xs mt-1">{p.desc}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>

    {/* Visit */}
    <section className="py-24 bg-gradient-hero text-primary-foreground">
      <div className="container mx-auto px-4 text-center">
        <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}>
          <MapPin size={40} className="mx-auto mb-4 opacity-80" />
          <h2 className="font-heading text-3xl font-bold mb-3">Rendez-nous visite</h2>
          <p className="opacity-80 mb-6 max-w-md mx-auto">Venez découvrir nos espaces et rencontrer notre communauté.</p>
          <Link
            to="/pricing"
            className="inline-flex items-center gap-2 bg-primary-foreground text-primary px-8 py-3 rounded-lg font-semibold hover:opacity-90 transition-opacity"
          >
            Réserver une visite <ArrowRight size={18} />
          </Link>
        </motion.div>
      </div>
    </section>

    <Footer />
  </div>
);

export default About;
